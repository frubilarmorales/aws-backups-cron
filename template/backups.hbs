<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Respaldo Completado</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: #f4f4f4;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            background: #ffffff;
            border: 1px solid #e0e0e0;
            box-shadow: 0px 5px 15px rgba(0, 0, 0, 0.1);
        }

        .header {
            background: #007bff;
            color: #fff;
            padding: 20px;
            text-align: center;
        }

        h1 {
            font-size: 28px;
            margin: 0;
        }

        .content {
            padding: 20px;
        }

        p {
            font-size: 18px;
            color: #333;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th, td {
            border: 1px solid #ccc;
            padding: 10px;
            text-align: left;
        }

        th {
            background: #f0f0f0;
        }

        .logo {
            max-width: 200px;
            height: auto;
            display: block;
            margin: 0 auto;
        }

        .footer {
            background: #007bff;
            color: #fff;
            text-align: center;
            padding: 10px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Respaldo Completado</h1>
        </div>
        <div class="content">
            <p>Los siguientes respaldos se han subido correctamente:</p>
            <table>
                <thead>
                    <tr>
                        <th>Nombre del Respaldo</th>
                
                        <th>Bucket</th>
                        <th>Estado de Carga</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each uploadedFilesInfo}}
                    <tr>
                        <td>{{this.name}}</td>
                      
                        <td>{{this.bucket}}</td>
                        <td>{{#if this.uploaded}}Subido{{else}}No subido{{/if}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
            <p>Total de archivos subidos: {{successfulUploads}} de {{totalFilesToUpload}}</p>
        </div>
     
        <div class="footer">
            <p>&copy; {{currentYear}} ITESICWS. Todos los derechos reservados.</p>
        </div>
    </div>
</body>
</html>
